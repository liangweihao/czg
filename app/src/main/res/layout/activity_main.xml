<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:clipChildren="false">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/radioGroup">

        <FrameLayout
            android:id="@+id/frameLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"/>

    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="@dimen/dimen_1dp"
        android:background="@color/color_d6d6d6"
        android:layout_above="@+id/radioGroup"/>

    <RadioGroup
        android:id="@+id/radioGroup"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dimen_50dp"
        android:layout_alignParentBottom="true"
        android:background="@color/white"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/main_production"
            style="@style/radioButton"
            android:drawableTop="@drawable/selector_tab_main_production"
            android:textSize="@dimen/dimen_9sp"
            android:text="AR施工图" />

        <RadioButton
            android:id="@+id/main_education"
            style="@style/radioButton"
            android:drawableTop="@drawable/selector_tab_main_education"
            android:textSize="@dimen/dimen_9sp"
            android:text="VR教育" />

        <RadioButton
            android:id="@+id/main_library"
            android:textSize="@dimen/dimen_9sp"
            style="@style/radioButton"/>

        <RadioButton
            android:id="@+id/main_find"
            style="@style/radioButton"
            android:drawableTop="@drawable/selector_tab_main_find"
            android:textSize="@dimen/dimen_9sp"
            android:text="发现" />

        <RadioButton
            android:id="@+id/main_my"
            style="@style/radioButton"
            android:drawableTop="@drawable/selector_tab_main_my"
            android:textSize="@dimen/dimen_9sp"
            android:text="我的" />

    </RadioGroup>

    <RelativeLayout
        android:id="@+id/rl_library_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="-0dp"
        android:clipChildren="false"
        tools:visibility="visible">


        <ImageView
            android:id="@+id/library_img"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:src="@drawable/selector_tab_main_library"/>

    </RelativeLayout>

</RelativeLayout>
